# 项目接续说明

更新日期：2026-09-25。本文件是全队与后续开发会话的共同入口，不代替当前用户指令。此前“只完成前端初始化、只编辑 frontend、固定某台电脑路径”等内容属于历史会话范围，不能作为当前全项目开发限制。

## 项目与事实来源

- 仓库：`flx6662007/Chuangxiang-Community`，创享科创资讯与参赛组队平台。
- 三人按前端、后端、数据库主责协作；数据库模型和迁移位于同一个 Django 工程，不是单独的数据库应用仓库。
- 前端为 Vue 3 / JavaScript / Vite，后端为 Django / DRF，数据库为 PostgreSQL。账号使用 django-allauth Headless + Session + CSRF。
- 当前实现与验证结果读 [团队进度](docs/progress.md)，启动读 [后端开发](docs/backend-development.md) 与 [前端开发](frontend/README.md)，接口读 [API 说明](docs/api.md)。
- 模型规则读 [数据库字段说明](docs/database-fields.md) 与配套字段表；[数据库交付说明](docs/database-handoff.md) 保留交付时记录，不把其中历史的“尚未开发”覆盖当前实际代码。

## 当前范围

当前实现包括赛事查询与管理、账号、固定模板组队完整事务与页面、站内通知、两个官方来源的采集适配器与定时命令。本机真实来源初始化、调度安装、测试与上传结果需看进度表，不把代码存在当成已经运行。邮箱验证码在开发终端读取，没有真实 SMTP 投递；AI 默认关闭；未公网部署。

用户本轮要求先做无需购买服务的版本，将之后可能付费的资源列清单。开发、测试、说明与部署准备可继续推进；不要据此自动购买服务或宣称已经上线。部署配置和边界见 [部署说明](docs/deployment.md)，资源见 [费用清单](docs/procurement.md)。

## 接续时先做

1. 在实际仓库检查当前分支、`git status`、最近提交和远端，不假设本机路径或旧分支状态仍然成立。
2. 查看本次任务对应源码与文档，区分已实现功能、模型基础和未来规划。
3. 保留其他成员未提交改动，确认编辑范围，避免多人同时改同一文件；不得重新初始化覆盖已有工程。
4. 执行与改动相关的验证，结果更新进度；没有运行的检查不要写“通过”。
5. 上传、PR、合并与公网部署分别确认实际结果。提交说明不包含真实密钥、学生资料或数据库备份。

本轮整合使用 `codex/backend-rounds`，接入数据库 `4f39834` 和前端 `970e83e`；最新是否上传/合并以实际 Git 与进度记录为准。已有前端分支确实叫 `frtend/round1`，不要擅自改写这个名称。

## 开发边界

- 保持 `AUTH_USER_MODEL='accounts.User'`；模型修改者生成迁移并与数据库负责人对齐，其他成员执行已有迁移，不重写迁移历史。
- 浏览器只调用后端 API，不直连 PostgreSQL；公开接口不返回私人联系方式和内部审核记录。
- 未验证账号允许登录。新增招募与申请必须由后端检查邮箱、限制、联系方式及具体对象条件；前端隐藏按钮不能代替权限。
- 赛事字段写入复用事务服务；来源核验、发布和下架不能直接改数据库状态绕过检查。有组队关系的变更使用 `lock_competition_graph`，保持用户→赛事→队伍的统一锁顺序，随后结算申请和联系权限；已入队关系保留。
- 当前只开放本人主邮箱核验，没有邮箱自助换绑；不在 User 再建验证布尔列，不手工标记核验成功。
- 赛事卡片读取已保存数据。采集和 AI 不放在浏览请求中；`ai_services` 没有公开 HTTP 路由，生成结果应由管理员确认。
- `.env`、`.venv/`、`.local/`、`node_modules/`、真实数据库内容留在本机。配置样例、依赖锁文件、模型迁移和共享文档提交仓库。

解释面向正在学习开发的团队，使用中文，先讲功能与实际操作，再介绍必要术语。不要把历史说明中的个人职责、旧测试数量或某台电脑状态当作全队现状。
