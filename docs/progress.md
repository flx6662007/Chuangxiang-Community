# 团队进度

**记录日期：2026-09-25。** 本文件是前端、后端、数据库和 AI 工作的统一进度入口。本次只更新已核实的数据库负责部分，其他负责人尚未共享的进度不作推测。

基础工程和共享文档已合入 `main`，团队可直接从仓库首页查看。本文件持续记录各方已共享的进度。

记录依据为当前工程和已完成的本机验证；尚未提交的本地成果单独标明。队友尚未共享的本地工作记为“待确认”，不据此判断其未开始。计划、代码已提交、本机验证通过、合并到主分支和部署上线是不同状态。

## 当前交付

| 部分 | 主负责人角色 | 已交付或已确认 | 当前状态 |
| --- | --- | --- | --- |
| 前端 | 前端负责人 | 已有前端技术方案；仓库预留 `frontend/` 入口 | 当前共享代码中没有页面实现；负责人本地进度待确认 |
| 后端 | 后端负责人 | Django/DRF 工程、配置读取、账号与赛事模块目录、健康检查、迁移保护、开发说明 | 骨架已合入 `main`，可启动；赛事和账号业务未实现 |
| 数据库 | 数据库负责人，后端负责人配合 | 用户、赛事与全部本轮确认的后续字段表/说明、模型、迁移、两轮虚构样例与导入、结构核验和交付说明 | 本机共 58 张表、35 项迁移；108 项 PostgreSQL 测试通过，独立空库完整迁移与重复导入通过；成果随数据库分支交付，后端审阅与合并待完成 |
| AI | 后端负责人维护服务包；评测负责人待团队确认 | `ai_services` 基础实现：配置、兼容模型调用、提示词、引文与字段校验、异常分类及离线测试 | 默认关闭外部调用；真实 API 联调、模型选型和业务接入待完成 |

后端已完成的验证记录：Django 系统检查通过、3 项骨架测试通过、实际 PostgreSQL 查询通过、健康接口实际 HTTP 请求通过。这些结果只覆盖当前骨架，不表示业务功能可用，也不表示每位队友的电脑均已配置完成。

本分支 `competitions/` 已包含模型和基础校验，本机现有明确标记的虚构样例，没有采集或导入真实赛事数据。初始迁移已完成；`/admin/` 仍需适配管理表单，模型和样例代码不代表管理与发布流程已可用。

## 下一步与验收

| 工作 | 主负责人角色 | 依赖或需对齐事项 | 完成标准 | 状态 |
| --- | --- | --- | --- | --- |
| 核对前端本地进度并提交基础工程 | 前端负责人 | 统一 `frontend/` 目录和启动说明 | 队友可按说明启动前端，能找到赛事列表页面入口 | 待同步 |
| 确定用户和赛事字段 | 数据库负责人，后端负责人配合 | 明确自定义用户、邮箱规则、赛事届次、字段类型及可空规则 | 字段含义达成一致，能够据此写模型与接口 | 用户字段 v1.0 已确认；赛事部分已审核通过，字段表与说明 v1.0 已交付；待同步后端核对技术取值与接口映射 |
| 实现模型和初始迁移 | 数据库负责人 | 先实现 `accounts.User` 并配置 `AUTH_USER_MODEL`，再生成迁移 | 迁移文件入库，空开发数据库可按迁移建立所需表 | 用户/赛事及后续模型的本机迁移、空库建表、样例与验收完成；迁移文件随本次提交交付，待团队审核 |
| 后续模块字段与数据库交付 | 数据库负责人，后端负责人配合 | 按本轮组队、科研、资源、快讯、收藏、通知、治理与追溯的确认规则 | 正式字段表及说明、模型与迁移一致，虚构样例可重复导入，交付说明回应原要求 | 本地完成；新增 44 张表；[交付说明](database-handoff.md)汇总实现和后续分工，事务接口接续由后端负责 |
| 补充赛事管理与测试数据 | 后端、数据库负责人 | 赛事模型和迁移完成 | 管理员可维护赛事，使用明确标记的测试数据验证流程 | 数据库样例及可重复导入已完成本机验证并随分支交付；Admin 管理流程待后端开发 |
| 实现公开赛事列表接口 | 后端负责人 | 赛事模型、公开字段、分页和排序约定 | API 文档与实际响应一致，只返回允许公开的数据 | 待开发 |
| 接入列表并完成联调 | 前端负责人，后端负责人配合 | 列表接口可调用，双方确认响应格式 | 页面从接口读取测试赛事并展示，加载、空数据和失败状态可处理 | 待开发 |
| 开展 AI 小样本验证 | 负责人待定 | 允许使用的官方通知样本、字段定义、原文依据与评测标准 | 能对比提取结果与人工标注，记录错误、耗时和费用，再决定模型 | 待安排或同步 |

第一阶段的交付目标是跑通：**测试赛事入库 → 后端接口读取 → 前端列表展示**。健康检查通过不等于这条流程已经完成。

此前通过 `database/round1-models` 分支交付 [用户字段表](user-fields-table.md)及其 [完整说明](database-fields.md#accounts)、[赛事字段表](competition-fields-table.md)及其 [完整说明](database-fields.md#competitions)、[账号与学校邮箱验证方案](backend-development.md#email-integration)，以及第一轮模型。初始化、样例与验收记录见 [数据库交付说明](database-handoff.md)。2026-09-25 已继续完成 [剩余模块字段表](remaining-fields-table.md)和 [说明](database-fields.md#teams)，新增 44 张表及 15 项迁移；当前共 58 张表、35 项迁移。PostgreSQL 108 项测试通过；SQLite 共 108 项，106 通过、2 项专用检查跳过。显式约束 209 个、普通索引 46 个已核对；独立空库从零迁移、两轮导入及重复导入通过，临时验证库已清理。

开发库现有 15 个禁用虚构用户、29 条赛事（26 公开、2 草稿、1 下架），以及科研/资源各 4 条、队伍/招募卡各 3 条、申请/成员各 6 条和关联快讯、通知、治理、采集追溯样例。两条导入命令都已验证重复执行保留已有内容，不发信或调用外部服务。完整成果、原要求逐项回应与后续交接见 [数据库开发交付说明](database-handoff.md)，机器可读结果见 [核验报告](database-verification-2026-09-25.json)。

本轮模型、迁移、样例、测试和正式文档随 `database/round1-models` 交付；本地讨论大纲不纳入提交。后端接续事务写入、权限、Admin 表单、发布和公开接口；详见 [开发说明](backend-development.md)。邮箱验证的业务规则和 allauth 状态归属已确认，实际依赖、验证码配置、发信及真实收件尚待后端接入。数据库本机验收不表示业务已联调或已合入 `main`。

后续再逐步实现学校邮箱核验、固定模板招募、申请与入队、资讯同步、AI 辅助和管理功能。本阶段不要求先把所有规划技术安装完成。

## 相关提交

| 记录日期 | 分支或 PR | 内容 | 当时状态 |
| --- | --- | --- | --- |
| 2026-09-24 | [`main`](https://github.com/flx6662007/Chuangxiang-Community/tree/main) · [PR #1](https://github.com/flx6662007/Chuangxiang-Community/pull/1) | 后端骨架、目录整理、共享进度及开发说明 | 已合入 `main`，作为团队共同开发起点；业务功能仍待开发 |
| 2026-09-25 | [`database/round1-models`](https://github.com/flx6662007/Chuangxiang-Community/tree/database/round1-models) | 确认字段表、模型建立与迁移、数据库初始化与交付 | 数据库分支交付；后端审阅、PR 与合并待完成 |

## 如何更新

- 各负责人在提交可共享成果时更新本文件，写清交付内容、剩余工作及依赖，附相关 PR 或文件链接。
- “已完成”应对应表中验收点；仅创建目录、写出方案或通过健康检查不能代替业务验收。
- 合并或完成联调后更新状态与记录日期；尚未核实的工作保留“待确认”，不填写推测的百分比。
