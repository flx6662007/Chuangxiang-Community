# 团队进度

**记录日期：2026-09-26。** 本文件统一记录三人共享成果。代码实现、本机验证、上传、合并和公网部署分别记录，不用百分比代替可检查的交付。

## 当前：组队与赛事采集

本轮已通过 [PR #5](https://github.com/flx6662007/Chuangxiang-Community/pull/5) 合入 `main`，功能提交为 `7634506`。67 个变更文件已与本机最终验证版本比对一致（仅规范化文本换行）；本机也已切回并同步主分支。此节是最新状态，下文保留上一版的交付记录。

| 部分 | 已实现内容 | 本机运行与待办 |
| --- | --- | --- |
| 前端 | 团队广场、详情、固定模板发布／编辑、我的组队、双方确认、退出／移除／解散、通知；赛事分类与关联组队 | 20 项前端测试及生产构建通过；隔离双账号已实际走通页面登录、发布、申请、编辑挂起、重新确认、入队、退出、撤销联系与通知已读 |
| 后端 | 完整组队事务、受控联系方式、版本变更、固定 24 小时结算、赛事下架联动、通知与管理员招募下架 | 最终 PostgreSQL 回归通过，覆盖最后名额、同人双队、编辑时确认、离队隐私与通知 |
| 数据库 | 复用现有全部模型，补选项初始化；保留原文版本、采纳记录和队伍历史 | 无新增模型字段，无新迁移；Django check 与 makemigrations --check 通过；本机已初始化 22 个招募选项 |
| 采集 | AIC、数维杯两种官方适配器；发现文章、提取字段、版本去重、规则采纳和重要变更待复核 | 真实网页可读取；23 项采集专项及最终整合回归通过，正式库尚未首次导入 |
| 调度 | 每 6 小时采集、每分钟结算的 Windows 脚本及日志，可分别安装 | 本机已启用每分钟结算，实际执行成功；采集任务尚未安装，等待首次入库。依赖本机开机、登录、数据库和网络可用 |

最终完整 PostgreSQL 回归 **212 项全部通过**（352.991 秒），前端 **20 项全部通过**并成功构建。采集已覆盖旧候选、原文回退、新届次复用网址、延期日期与师生人数歧义；Windows 两份脚本通过 PowerShell 5.1 语法解析。`pip check` 无依赖冲突，模型无需新迁移。

此后新增的 **1 项“拒绝针对虚构赛事发布招募或提交申请”专项测试已单独通过**。这是完整回归之后的增量验证，尚未重新执行包含该测试的整轮回归，不记作“213 项完整回归通过”。

**当前初始化待办：**本机没有赛事管理员；采集专用账号的授权待确认。完成授权后才能记录实际采纳人、首次导入真实赛事并启用采集定时任务。目前不宣称采集已持续运行。公共读取排除固定种子编码且带虚构标记的旧数据，后台历史保留；正常页面在真实入库前显示空态。

**本机结算运行：**`Chuangxiang-TeamSettlement` 使用当前 Windows 用户、交互登录与普通权限，每分钟调用一次结算命令。2026-09-26 已核对任务实际执行成功；17:15:35 开始的一次运行于 17:15:41 结束，日志为 `exit=0`，中文输出可读，后续周期已安排。该任务不负责采集，不代表真实赛事已入库。日志见仓库 `.local/maintenance/settle-latest.log`（不上传）。

**页面验收：**详见[双账号浏览器记录](browser-acceptance-2026-09-26.md)。测试使用独立库和两个普通账号，不向正式开发库添加虚构赛事。已修正已结束请求的“待我回应”标签；前端 20 项测试及构建再次通过。

**非交互账号保护：**已禁止不可用密码账号通过找回密码、旧验证码或修改密码入口启用登录，正常学生流程保留。新增 6 项针对性回归，`accounts` 专项 36 项全部通过（26.765 秒），本机后端已重启加载。拟使用的采集账号仅需新增赛事、修改赛事、新增来源三项权限，仍需负责人明确授权，尚未创建；不会授予超级管理员、账号管理或删除权限。

**团队交接：**前端按[组队接口](api-teams.md)联调两个已核验账号，后端执行首次采集并处理重要变更候选，数据库同学复核已有约束和字段证据。实现方法见[前端实现](frontend-implementation.md)、[赛事采集](ingestion-implementation.md)、[本机定时任务](maintenance.md)。SMTP、公网部署、AI、科研快讯和资源栏目仍暂缓。

## 历史记录：2026-09-26 界面与方案修订

根据团队提供的蓝白界面参考图，调整现有首页、赛事中心、详情与账号页面，并增加真实 404 页面与返回入口。首页使用原创 SVG 校园插画和现有接口返回的最新 4 条赛事；列表和详情采用统一卡片与分区。保留实际搜索、分页、来源和账号流程，不添加无依据的统计、报名状态、讨论区或未接通的功能按钮。

新增 [界面与功能修订](ui-plan.md)、[前端方案](frontend-plan.md)、[后端方案](backend-plan.md)和[下一轮团队任务](next-steps.md)，并在 README 加入入口。网站和当前共享文档统一使用“创新俱乐部”，移除限定书院的展示名称。下一步以参赛组队和赛事采集为 M1，科研快讯、资源和收藏为 M2，有来源的智能助手为 M3。暂停真实 SMTP 接入，已有认证与权限保留。

本次界面、方案与分工已通过 [PR #4](https://github.com/flx6662007/Chuangxiang-Community/pull/4) 合入 `main`，合并提交为 `aa12ef2`。18 个变更文件已与本机验证版本比对一致（文本换行差异除外），远端主分支与整合分支内容一致，全队可直接从主分支获取。本次只调整前端和实施文档，未新增组队、真实采集或 AI 问答接口；下面 PR #3 的合并记录仅指此前两轮业务基线。

本机浏览器已检查 1440 像素宽屏下首页、列表、详情和账号布局；验证搜索命中与空结果、第二页 6 条样例、详情返回保留搜索条件、注册表单切换及 404 返回入口。独立代码复核发现辅助文字偏小偏浅，已调整关键日期、来源和表单说明为至少 12px 并加深颜色。本轮未重新注册真实账号或发送邮件。

本次验证：现有前端 **12 项测试通过**，最终 **Vite 生产构建通过**，`git diff --check` 无空白错误；首页搜索跳转及查询结果已实际验证，所检查浏览器标签未记录 JavaScript 错误。本轮未修改后端，不重复执行此前 148 项后端测试，也不把历史结果算作本轮重新验证。

## 历史记录：前两轮结论（赛事与账号）

以下为组队与采集开发前的历史快照。“尚未实现”“等待接入”等只描述当时状态，最新进展以上方“当前：组队与赛事采集”为准。

第一轮的赛事浏览与内容管理、第二轮的账号与学校邮箱核验已完成本地实现。PostgreSQL 独立测试库完整测试 **148 项通过**，前端 **12 项测试及构建通过**，本机浏览器核心操作已验证。

两轮成果已通过 [PR #3](https://github.com/flx6662007/Chuangxiang-Community/pull/3) 合入 `main`，功能合并提交为 `987ce81`。已核对远端代码与本机测试版本一致（文本换行差异除外）；各成员可从主分支获取共同基线。

当前先做**无需购买服务的开发版**：本机运行，控制台读取验证码，AI 默认关闭。本轮没有购买服务器、域名或模型服务，没有向真实学校邮箱发信，也未公网部署。后续资源准备见 [部署说明](deployment.md) 与 [资源清单](procurement.md)。

## 历史记录：前两轮交付

| 轮次 | 工作 | 当前成果 | 实现边界 |
| --- | --- | --- | --- |
| 第一轮 | 数据库基础 | 接续同学交付的字段表、模型、迁移、两轮虚构样例及检查命令 | 原始交付见 [数据库交付说明](database-handoff.md)，模型存在不代表所有业务已上线 |
| 第一轮 | 赛事 API | 公开列表、搜索、分类筛选、分页、详情；草稿/下架不公开，来源及字段白名单 | 没有自动采集真实赛事 |
| 第一轮 | 内容后台 | 草稿、分类标签、来源核验、发布、理由下架及审计；主来源切换走事务 | 已关联队伍的赛事暂拒绝下架，等待状态联动 |
| 第一轮 | 前端展示 | 首页、赛事列表与详情；搜索分页、原文链接、加载/空/失败状态 | 分类筛选 API 已有，页面暂无分类控件；没有招募页面 |
| 第二轮 | 账号与验证码 | 学校邮箱注册/登录/退出、密码找回、Session/CSRF、验证码与限流 | 未验证用户可登录；真实发信尚未配置，开发邮件只在终端显示 |
| 第二轮 | 本人资料 | 维护本人微信号、手机号，读取核验和账号资格状态 | 不开放邮箱自助换绑，公开接口不返回联系方式 |
| 第二轮 | 账号门槛 | 复用权限检查：账号启用、当前学校邮箱验证、有效限制、至少一种联系方式 | 招募/申请 API 尚未实现，后续还需检查队伍、赛事和名额 |
| 后续准备 | AI | 已有模型调用、提示词、校验、异常处理及离线测试 | 默认关闭；没有真实模型评测、HTTP 入口或自动发布 |
| 后续准备 | 部署 | Linux、Docker Compose、Caddy、Gunicorn、PostgreSQL、Redis、SMTP 的配置与说明 | 本机无 Docker，未构建运行容器；无域名和发件服务，未验证真实部署 |

数据库基础样例为 15 个禁用虚构用户、29 条赛事（26 公开、2 草稿、1 下架），并有后续模块关联样例。数据只在各自开发库中；GitHub 不同步数据库记录。人工测试可能增加记录，不将后续数量变化当作导入失败。

## 历史记录：前两轮验证

| 范围 | 2026-09-25 结果 | 边界 |
| --- | --- | --- |
| PostgreSQL 自动化 | 在独立 `test_chuangxiang_dev` 测试库执行完整测试，148 项全部通过，30.617 秒，无跳过；系统检查无问题 | 覆盖当前模型、赛事服务/API/后台和账号流程；使用测试邮件，不验证真实 SMTP |
| 独立 AI 代码审查 | 已检查赛事、账号、前端与部署衔接，并修复注册失败回滚、重发次数保持、后台账号核验衔接、主来源切换表单冲突 | 不替代实际浏览器操作、真实收件与目标服务器验证 |
| 前端测试与构建 | 接入队友最新卡片与数字分页后，12 项测试全部通过，生产构建通过 | 覆盖格式处理与会话边界；构建成功不等于公网已部署 |
| 浏览器联调 | 本机实际验证：26 条公开样例、第二页、搜索空结果、赛事详情、注册、错误码拒绝、正确码核验、联系方式、登录退出、跨标签退出后旧页面恢复登录入口 | 后台发布等由真实 HTTP 自动化测试覆盖；密码找回通过接口回归，仍建议全队按人工验收表复测；没有真实邮件投递 |
| 部署静态检查 | 配置文件、变量、挂载路径和初始化脚本已检查，详见部署说明 | 未执行 Docker 构建/运行，也未获得真实 HTTPS 证书 |

数据库同学此前交付的“108 项 PostgreSQL 测试、58 张表、35 项迁移”是当时模型阶段记录；本轮增加 allauth 依赖与迁移、业务测试，不继续把旧数字作为当前全项目规模。[历史核验报告](database-verification-2026-09-25.json) 保留原结果。

## 历史记录：前两轮 Git 交接

| 来源 | 内容 | 本轮状态 |
| --- | --- | --- |
| `main`，功能合并 `987ce81` | 数据库基础、两轮业务、前端页面和本次共享文档 | 已合入；可按开发说明启动，无需切换到队友分支获取本轮成果 |
| `database/round1-models`，`4f39834` | 数据库完整模型、迁移、样例与说明 | 已经整合并随 PR #3 合入主分支 |
| `frtend/round1`，`970e83e` | 前端工程、卡片组件与初始分页 | 已接入整合分支；保留原分支拼写和组件，接续真实 API 数据 |
| [PR #2](https://github.com/flx6662007/Chuangxiang-Community/pull/2) | 将前端分支合入 `codex/backend-rounds` | 已合入远端整合分支；不是合入 `main` |
| [PR #3](https://github.com/flx6662007/Chuangxiang-Community/pull/3)，`codex/backend-rounds` | 两轮业务实现、联调、部署准备与本次文档 | 已完成独立 AI 审查、自动化测试、本机页面检查、上传比对，并合入 `main`；未公网部署 |

本轮未改变既有字段基线，也未要求数据库同学另写一套后端。数据库同学继续维护模型和迁移，后端接续事务、权限与 API，前端依据 API 展示页面；共享契约在 [API 说明](api.md)。各自克隆完整仓库，在自己的任务分支工作，使用自己的 `.env` 和开发数据。

## 历史计划：前两轮交付后的待办

以下保留前两轮结束时的工作计划；其中组队、通知、赛事联动与采集代码已在本轮实现，不再视为当前未开发功能。当前初始化、联调和运行待办以上方最新状态为准，原任务依据见[团队任务清单](next-steps.md)。

| 顺序 | 主负责人 | 工作与完成标准 |
| --- | --- | --- |
| 1 | 全队 | 按 [人工验收](manual-checks.md) 启动各自环境，验证赛事浏览、邮箱验证码、联系方式和密码找回；记录无法复现的步骤 |
| 2 | 后端，数据库配合 | 依据已确认模型实现固定模板招募、申请及入队事务；检查邮箱、联系方式、限制、赛事有效性、重复申请和名额并发 |
| 3 | 前端，后端配合 | 开发招募卡、发布表单、申请/处理界面；使用已约定错误和状态，与后端一起验收完整流程 |
| 4 | 后端，数据库配合 | 接续赛事下架对已有队伍的影响、通知与审计，解除当前关联队伍下架保护前先完成状态测试 |
| 5 | 团队共同确定内容负责人 | 确认可接入官方来源和公众号同步方式，建立来源更新、去重及管理员确认流程，再接入真实内容 |
| 6 | 后端牵头，前端/数据库配合 | 用授权样本评测通知提取与快讯草稿，记录准确性、漏项、耗时及费用；确认后才启用模型 |
| 7 | 团队协调资源 | 优先申请学校服务器、子域名、发件服务；确需付费时按资源清单确认后采购，完成目标服务器和真实收件验收后开放 |

本轮“免费”指无需新增购买服务，仍需成员投入开发维护时间。当前不必买 GPU、托管数据库、对象存储或 CDN。科研、快讯、资源、收藏、通知与治理等后续模块按功能逐步实现，不把完整模型表一次性开放为公共写接口。

## 更新方式

各负责人提交共享成果时写清“做了什么、如何验证、剩余工作、相关提交或 PR”。合并后补真实链接和提交号，上线后另补部署环境、日期与实际验收记录。尚未共享的队友本地工作记为待确认，不据此判断对方没有开始。
